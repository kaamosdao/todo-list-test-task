import React from 'react';

const Button: React.FC = () => (
  <button
    className="btn btn-outline-secondary input-group__button"
    type="submit"
    id="button-addon2"
  >
    Add
  </button>
);

export default Button;
